## Recent
[chat] Theme content area maxes at 1440px (page_width="narrow" = 90rem) minus 80px margins, so at 1024-1400px viewport the usable width is only ~944-1320px. Desktop layouts sized for the full 1440px (fixed-px multi-column grids/flex with no shrink) overflow this band.
[chat] A single overflowing section (e.g. product-categories with flex-wrap:nowrap + 5x265px images) creates a page-wide horizontal scrollbar, which makes EVERY section look shifted/non-responsive. When "all sections are not responsive" in a width band, hunt for one overflowing section first. Fix: flex:1 1 0 + min-width:0 on items, width:100% + max-width on images (keeps the 1440px look, shrinks below).
[chat] Swapping a section's breakpoint number (750px to 1024px) does NOT make it responsive in the new band — it just moves where the fixed desktop layout begins. Must also make that desktop layout fluid (fr/flex/percent) for the 1024-1400px range.
[chat] Metaobject definitions must be created with access.storefront = PUBLIC_READ, otherwise `shop.metaobjects.<type>[handle]` returns nil in Liquid even though entries exist. The `ingredient_detail` type referenced by an earlier ingredients-drawer build was never actually created on the store, so the drawer rendered empty.
[chat] file_reference metaobject fields require a MediaImage GID (gid://shopify/MediaImage/...) as the value — query `files(query:"...")` to fetch GIDs for existing shop_images. Add validation file_type_options=["Image"] to restrict to images.
[chat] Ingredient drawer data lives in the `ingredient` metaobject (fields: title, description, image). Cards on page.ingredients.json link via the `ingredient_handle` block setting; drawer image falls back to the card's own image when the metaobject image is blank.
[chat] The Figma "Contact Directory" page (node 7439-30071) = country office IMAGE cards (image + country + phone + email + "Contact Support" button, rendered by contact-offices.liquid) + "Wherever You Are, We're Here to Help" help section (contact-help.liquid). It is NOT text-only cards. The contact-directory.liquid text-card section is a wrong early build and is unused — page.contact-directory.json must use contact-offices + contact-help (same config as page.contact.json).
[chat] "Images & Curated Reviews" (Figma node 5115-21162 desktop / 5709-27341 mobile) is a SINGLE-review CAROUSEL, NOT a grid of images + review cards. Each slide = product image (top, rounded ~8px) → centered 5-star rating (BLACK #141414, not gold) → centered review quote (24px) → reviewer name ("-Name") → centered prev/next circular nav buttons + pagination dots. Card is rounded 16px, centered, padding 39/24/80/24 desktop, 24/16/40/16 mobile. Mirrors the existing reviews-carousel.liquid pattern but adds an image per slide. The earlier grid build was wrong.
[chat] Hero section corner radius: rounding only .hero__container (overflow:hidden) does NOT reliably clip the hero image — the media lives in an absolutely-positioned .hero__media-grid. Apply border-radius + overflow:hidden directly to .hero__media-grid to round the image.
[chat] Hero image height: media fills the section via absolute inset:0, so image height == hero min-height. To give a px height control, add an image_height range (px) that overrides --hero-min-height when > 0, falling back to the section_height option when 0.
[chat] Shopify Liquid CANNOT read URL query parameters (no request.query_string). To make one page show different content per ?param=value, render all variants as blocks and toggle visibility with a small inline <script> reading new URLSearchParams(location.search). Used for contact-country: one /pages/contact-country page holds 8 country blocks; office cards link with ?country=<country_name|handleize> and JS shows the matching panel.
[chat] Shopify contact form POST drops the query string on the success redirect (it appends ?contact_posted=true to request.path). When a JS-toggled multi-panel page relies on ?country=, also detect the panel containing a .form-success/.form-errors element and show THAT panel first, so the right country stays visible after submit.
[chat] The contact-country page is reached at /pages/contact-country (a real Page resource, handle contact-country, template suffix contact-country, created via pageCreate). Office-card buttons (contact-offices.liquid) build URLs from a `directory_page` url setting (fallback /pages/contact-country) + '?country=' + country_name|handleize.
[chat] Brand directory Figma (node 5368-22248) is titled "Brand Directory 1 (mixed grid)": under the "Our Brands" h2, each category (Health & Nutrition, Beauty, Jewelry, Other) is a heading + a 4-col GRID of full-bleed square image cards (345.5px, 10px gap), where some cards span 2 columns (wide, 701.5px). It is NOT horizontal carousels. brand-category-grid.liquid (heading + grid + per-card "wide" toggle) is the correct section; brand-category-carousel.liquid was a wrong earlier interpretation. page.brand-directory-3.json uses brand-category-grid.
